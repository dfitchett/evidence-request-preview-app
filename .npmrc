# Minimum release age for security - apply only to direct dependencies
# by excluding all scoped packages and known stable transitive deps
minimum-release-age=14d

# Exclude all scoped packages
minimum-release-age-exclude[]=@*/*

# Exclude common transitive dependencies (using wildcards where possible)
minimum-release-age-exclude[]=*
